{
  "id": "lead-agent",
  "name": "Lead Agent",
  "description": "Finds 25 people who match your ICP every morning and writes the first line of each email itself.",
  "replaces": ["Clay", "Instantly", "an SDR"],
  "frequency": "0 8 * * 1-5",
  "model": "gpt-4o-mini",
  "temperature": 0.6,
  "maxIterations": 10,
  "tools": ["prospector", "outreach"],
  "prompts": ["system", "email"],
  "scheduledTask": "Find 25 new people matching the ICP, then write a personalised first line and a short opening email for the 10 strongest matches.",
  "settings": [
    {
      "key": "icp",
      "label": "Who you want to reach",
      "type": "textarea",
      "placeholder": "Heads of Growth at 20-200 person B2B SaaS companies in the US who already run outbound",
      "help": "Plain English. The agent turns this into an Apollo search itself.",
      "required": true
    },
    {
      "key": "offer",
      "label": "What you are selling",
      "type": "textarea",
      "placeholder": "AgentStack — 3 AI marketing agents deployed in one click, $29 once",
      "required": true
    },
    {
      "key": "senderName",
      "label": "Your name",
      "type": "text",
      "placeholder": "Ankit",
      "required": true
    },
    {
      "key": "dailyLimit",
      "label": "Leads per run",
      "type": "select",
      "options": ["10", "25", "50"],
      "default": "25"
    },
    {
      "key": "tone",
      "label": "Email tone",
      "type": "select",
      "options": ["Direct", "Casual", "Formal"],
      "default": "Direct"
    }
  ],
  "secrets": [
    {
      "key": "OPENAI_API_KEY",
      "label": "OpenAI API key",
      "help": "Used to write the emails. Encrypted before it is stored.",
      "required": true
    },
    {
      "key": "APOLLO_API_KEY",
      "label": "Apollo API key",
      "help": "Used to find and enrich the leads. Encrypted before it is stored.",
      "required": true
    }
  ]
}
